
export const Button =  ({ children }: { children: React.ReactNode }) => {
    return (
        <div className="liquidGlassButton px-[14px] py-[6px] w-fit">
            {children}
        </div>
    )
}